import React from "react";
import NavbarBootstrap from "../components/Navbar";

const NavbarApp = () => {
    return (
        <div>
            <NavbarBootstrap />
        </div>
    );
};

export default NavbarApp;